naftiko: 1.0.0-alpha2
info:
  label: DocuSign REST API — TemplateHtmlDefinitions
  description: 'DocuSign REST API — TemplateHtmlDefinitions. 1 operations. Lead operation: TemplateHtmlDefinitions. Self-contained
    Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - TemplateHtmlDefinitions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: docusign-templatehtmldefinitions
    baseUri: https://www.docusign.net/restapi
    description: DocuSign REST API — TemplateHtmlDefinitions business capability. Self-contained, no shared references.
    resources:
    - name: v2-accounts-accountId-templates-templateId-html_definitions
      path: /v2/accounts/{accountId}/templates/{templateId}/html_definitions
      operations:
      - name: responsivehtmlgettemplatehtmldefinitions
        method: GET
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountId
          in: path
          type: string
          description: The external account number (int) or account id GUID.
          required: true
        - name: templateId
          in: path
          type: string
          description: The ID of the template being accessed.
          required: true
  exposes:
  - type: rest
    namespace: docusign-templatehtmldefinitions-rest
    port: 8080
    description: REST adapter for DocuSign REST API — TemplateHtmlDefinitions. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v2/accounts/{accountid}/templates/{templateid}/html-definitions
      name: v2-accounts-accountid-templates-templateid-html-definitions
      description: REST surface for v2-accounts-accountId-templates-templateId-html_definitions.
      operations:
      - method: GET
        name: responsivehtmlgettemplatehtmldefinitions
        description: responsivehtmlgettemplatehtmldefinitions
        call: docusign-templatehtmldefinitions.responsivehtmlgettemplatehtmldefinitions
        with:
          accountId: rest.accountId
          templateId: rest.templateId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: docusign-templatehtmldefinitions-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign REST API — TemplateHtmlDefinitions. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: responsivehtmlgettemplatehtmldefinitions
      description: responsivehtmlgettemplatehtmldefinitions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: docusign-templatehtmldefinitions.responsivehtmlgettemplatehtmldefinitions
      with:
        accountId: tools.accountId
        templateId: tools.templateId
      outputParameters:
      - type: object
        mapping: $.
