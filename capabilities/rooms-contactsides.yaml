naftiko: 1.0.0-alpha2
info:
  label: DocuSign Rooms API - v2 — ContactSides
  description: 'DocuSign Rooms API - v2 — ContactSides. 1 operations. Lead operation: Docusign Gets contact sides.. Self-contained
    Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - ContactSides
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rooms-contactsides
    baseUri: https://rooms.docusign.com/restapi
    description: DocuSign Rooms API - v2 — ContactSides business capability. Self-contained, no shared references.
    resources:
    - name: v2-contact_sides
      path: /v2/contact_sides
      operations:
      - name: contactsidesgetcontactsides
        method: GET
        description: Docusign Gets contact sides.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DOCUSIGN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rooms-contactsides-rest
    port: 8080
    description: REST adapter for DocuSign Rooms API - v2 — ContactSides. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v2/contact-sides
      name: v2-contact-sides
      description: REST surface for v2-contact_sides.
      operations:
      - method: GET
        name: contactsidesgetcontactsides
        description: Docusign Gets contact sides.
        call: rooms-contactsides.contactsidesgetcontactsides
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rooms-contactsides-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign Rooms API - v2 — ContactSides. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: docusign-gets-contact-sides
      description: Docusign Gets contact sides.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rooms-contactsides.contactsidesgetcontactsides
      outputParameters:
      - type: object
        mapping: $.
