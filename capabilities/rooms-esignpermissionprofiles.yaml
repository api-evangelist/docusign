naftiko: 1.0.0-alpha2
info:
  label: DocuSign Rooms API - v2 — ESignPermissionProfiles
  description: 'DocuSign Rooms API - v2 — ESignPermissionProfiles. 1 operations. Lead operation: Docusign Gets eSignature
    Permission Profiles.. Self-contained Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - ESignPermissionProfiles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rooms-esignpermissionprofiles
    baseUri: https://rooms.docusign.com/restapi
    description: DocuSign Rooms API - v2 — ESignPermissionProfiles business capability. Self-contained, no shared references.
    resources:
    - name: v2-accounts-accountId-esign_permission_profiles
      path: /v2/accounts/{accountId}/esign_permission_profiles
      operations:
      - name: esignpermissionprofilesgetesignpermissionprofiles
        method: GET
        description: Docusign Gets eSignature Permission Profiles.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountId
          in: path
          type: string
          description: The globally unique identifier (GUID) for the account.
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DOCUSIGN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rooms-esignpermissionprofiles-rest
    port: 8080
    description: REST adapter for DocuSign Rooms API - v2 — ESignPermissionProfiles. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v2/accounts/{accountid}/esign-permission-profiles
      name: v2-accounts-accountid-esign-permission-profiles
      description: REST surface for v2-accounts-accountId-esign_permission_profiles.
      operations:
      - method: GET
        name: esignpermissionprofilesgetesignpermissionprofiles
        description: Docusign Gets eSignature Permission Profiles.
        call: rooms-esignpermissionprofiles.esignpermissionprofilesgetesignpermissionprofiles
        with:
          accountId: rest.accountId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rooms-esignpermissionprofiles-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign Rooms API - v2 — ESignPermissionProfiles. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: docusign-gets-esignature-permission-profiles
      description: Docusign Gets eSignature Permission Profiles.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rooms-esignpermissionprofiles.esignpermissionprofilesgetesignpermissionprofiles
      with:
        accountId: tools.accountId
      outputParameters:
      - type: object
        mapping: $.
