naftiko: 1.0.0-alpha2
info:
  label: DocuSign Rooms API - v2 — RoomContactTypes
  description: 'DocuSign Rooms API - v2 — RoomContactTypes. 1 operations. Lead operation: Docusign Gets room contact types..
    Self-contained Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - RoomContactTypes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rooms-roomcontacttypes
    baseUri: https://rooms.docusign.com/restapi
    description: DocuSign Rooms API - v2 — RoomContactTypes business capability. Self-contained, no shared references.
    resources:
    - name: v2-room_contact_types
      path: /v2/room_contact_types
      operations:
      - name: roomcontacttypesgetroomcontacttypes
        method: GET
        description: Docusign Gets room contact types.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DOCUSIGN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rooms-roomcontacttypes-rest
    port: 8080
    description: REST adapter for DocuSign Rooms API - v2 — RoomContactTypes. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v2/room-contact-types
      name: v2-room-contact-types
      description: REST surface for v2-room_contact_types.
      operations:
      - method: GET
        name: roomcontacttypesgetroomcontacttypes
        description: Docusign Gets room contact types.
        call: rooms-roomcontacttypes.roomcontacttypesgetroomcontacttypes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rooms-roomcontacttypes-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign Rooms API - v2 — RoomContactTypes. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: docusign-gets-room-contact-types
      description: Docusign Gets room contact types.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rooms-roomcontacttypes.roomcontacttypesgetroomcontacttypes
      outputParameters:
      - type: object
        mapping: $.
