naftiko: 1.0.0-alpha2
info:
  label: DocuSign Rooms API - v2 — TransactionSides
  description: 'DocuSign Rooms API - v2 — TransactionSides. 1 operations. Lead operation: Docusign Gets transaction sides..
    Self-contained Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - TransactionSides
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rooms-transactionsides
    baseUri: https://rooms.docusign.com/restapi
    description: DocuSign Rooms API - v2 — TransactionSides business capability. Self-contained, no shared references.
    resources:
    - name: v2-transaction_sides
      path: /v2/transaction_sides
      operations:
      - name: transactionsidesgettransactionsides
        method: GET
        description: Docusign Gets transaction sides.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DOCUSIGN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rooms-transactionsides-rest
    port: 8080
    description: REST adapter for DocuSign Rooms API - v2 — TransactionSides. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v2/transaction-sides
      name: v2-transaction-sides
      description: REST surface for v2-transaction_sides.
      operations:
      - method: GET
        name: transactionsidesgettransactionsides
        description: Docusign Gets transaction sides.
        call: rooms-transactionsides.transactionsidesgettransactionsides
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rooms-transactionsides-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign Rooms API - v2 — TransactionSides. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: docusign-gets-transaction-sides
      description: Docusign Gets transaction sides.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rooms-transactionsides.transactionsidesgettransactionsides
      outputParameters:
      - type: object
        mapping: $.
