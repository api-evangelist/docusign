naftiko: 1.0.0-alpha2
info:
  label: DocuSign Rooms API - v2 — TaskStatuses
  description: 'DocuSign Rooms API - v2 — TaskStatuses. 1 operations. Lead operation: Docusign Retrieves the list of valid
    task statuses.. Self-contained Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - TaskStatuses
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rooms-taskstatuses
    baseUri: https://rooms.docusign.com/restapi
    description: DocuSign Rooms API - v2 — TaskStatuses business capability. Self-contained, no shared references.
    resources:
    - name: v2-task_statuses
      path: /v2/task_statuses
      operations:
      - name: taskstatusesgettaskstatuses
        method: GET
        description: Docusign Retrieves the list of valid task statuses.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DOCUSIGN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rooms-taskstatuses-rest
    port: 8080
    description: REST adapter for DocuSign Rooms API - v2 — TaskStatuses. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v2/task-statuses
      name: v2-task-statuses
      description: REST surface for v2-task_statuses.
      operations:
      - method: GET
        name: taskstatusesgettaskstatuses
        description: Docusign Retrieves the list of valid task statuses.
        call: rooms-taskstatuses.taskstatusesgettaskstatuses
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rooms-taskstatuses-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign Rooms API - v2 — TaskStatuses. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: docusign-retrieves-list-valid-task
      description: Docusign Retrieves the list of valid task statuses.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rooms-taskstatuses.taskstatusesgettaskstatuses
      outputParameters:
      - type: object
        mapping: $.
