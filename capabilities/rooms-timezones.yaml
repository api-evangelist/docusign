naftiko: 1.0.0-alpha2
info:
  label: DocuSign Rooms API - v2 — TimeZones
  description: 'DocuSign Rooms API - v2 — TimeZones. 1 operations. Lead operation: Docusign Gets time zones.. Self-contained
    Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - TimeZones
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rooms-timezones
    baseUri: https://rooms.docusign.com/restapi
    description: DocuSign Rooms API - v2 — TimeZones business capability. Self-contained, no shared references.
    resources:
    - name: v2-time_zones
      path: /v2/time_zones
      operations:
      - name: timezonesgettimezones
        method: GET
        description: Docusign Gets time zones.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DOCUSIGN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: rooms-timezones-rest
    port: 8080
    description: REST adapter for DocuSign Rooms API - v2 — TimeZones. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v2/time-zones
      name: v2-time-zones
      description: REST surface for v2-time_zones.
      operations:
      - method: GET
        name: timezonesgettimezones
        description: Docusign Gets time zones.
        call: rooms-timezones.timezonesgettimezones
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rooms-timezones-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign Rooms API - v2 — TimeZones. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: docusign-gets-time-zones
      description: Docusign Gets time zones.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rooms-timezones.timezonesgettimezones
      outputParameters:
      - type: object
        mapping: $.
