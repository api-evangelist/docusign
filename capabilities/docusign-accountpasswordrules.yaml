naftiko: 1.0.0-alpha2
info:
  label: DocuSign REST API — AccountPasswordRules
  description: 'DocuSign REST API — AccountPasswordRules. 3 operations. Lead operation: Docusign Get the password rules. Self-contained
    Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - AccountPasswordRules
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: docusign-accountpasswordrules
    baseUri: https://www.docusign.net/restapi
    description: DocuSign REST API — AccountPasswordRules business capability. Self-contained, no shared references.
    resources:
    - name: v2-accounts-accountId-settings-password_rules
      path: /v2/accounts/{accountId}/settings/password_rules
      operations:
      - name: accountpasswordrulesgetaccountpasswordrules
        method: GET
        description: Docusign Get the password rules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountId
          in: path
          type: string
          description: The external account number (int) or account id GUID.
          required: true
      - name: accountpasswordrulesputaccountpasswordrules
        method: PUT
        description: Docusign Update the password rules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountId
          in: path
          type: string
          description: The external account number (int) or account id GUID.
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v2-current_user-password_rules
      path: /v2/current_user/password_rules
      operations:
      - name: passwordrulesgetpasswordrules
        method: GET
        description: Docusign Get membership account password rules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: docusign-accountpasswordrules-rest
    port: 8080
    description: REST adapter for DocuSign REST API — AccountPasswordRules. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v2/accounts/{accountid}/settings/password-rules
      name: v2-accounts-accountid-settings-password-rules
      description: REST surface for v2-accounts-accountId-settings-password_rules.
      operations:
      - method: GET
        name: accountpasswordrulesgetaccountpasswordrules
        description: Docusign Get the password rules
        call: docusign-accountpasswordrules.accountpasswordrulesgetaccountpasswordrules
        with:
          accountId: rest.accountId
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: accountpasswordrulesputaccountpasswordrules
        description: Docusign Update the password rules
        call: docusign-accountpasswordrules.accountpasswordrulesputaccountpasswordrules
        with:
          accountId: rest.accountId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v2/current-user/password-rules
      name: v2-current-user-password-rules
      description: REST surface for v2-current_user-password_rules.
      operations:
      - method: GET
        name: passwordrulesgetpasswordrules
        description: Docusign Get membership account password rules
        call: docusign-accountpasswordrules.passwordrulesgetpasswordrules
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: docusign-accountpasswordrules-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign REST API — AccountPasswordRules. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: docusign-get-password-rules
      description: Docusign Get the password rules
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: docusign-accountpasswordrules.accountpasswordrulesgetaccountpasswordrules
      with:
        accountId: tools.accountId
      outputParameters:
      - type: object
        mapping: $.
    - name: docusign-update-password-rules
      description: Docusign Update the password rules
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: docusign-accountpasswordrules.accountpasswordrulesputaccountpasswordrules
      with:
        accountId: tools.accountId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: docusign-get-membership-account-password
      description: Docusign Get membership account password rules
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: docusign-accountpasswordrules.passwordrulesgetpasswordrules
      outputParameters:
      - type: object
        mapping: $.
