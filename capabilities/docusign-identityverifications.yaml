naftiko: 1.0.0-alpha2
info:
  label: DocuSign REST API — IdentityVerifications
  description: 'DocuSign REST API — IdentityVerifications. 1 operations. Lead operation: IdentityVerifications. Self-contained
    Naftiko capability covering one Docusign business surface.'
  tags:
  - Docusign
  - IdentityVerifications
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DOCUSIGN_API_KEY: DOCUSIGN_API_KEY
capability:
  consumes:
  - type: http
    namespace: docusign-identityverifications
    baseUri: https://www.docusign.net/restapi
    description: DocuSign REST API — IdentityVerifications business capability. Self-contained, no shared references.
    resources:
    - name: v2-accounts-accountId-identity_verification
      path: /v2/accounts/{accountId}/identity_verification
      operations:
      - name: accountidentityverificationgetaccountidentityverification
        method: GET
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: accountId
          in: path
          type: string
          description: The external account number (int) or account id GUID.
          required: true
  exposes:
  - type: rest
    namespace: docusign-identityverifications-rest
    port: 8080
    description: REST adapter for DocuSign REST API — IdentityVerifications. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v2/accounts/{accountid}/identity-verification
      name: v2-accounts-accountid-identity-verification
      description: REST surface for v2-accounts-accountId-identity_verification.
      operations:
      - method: GET
        name: accountidentityverificationgetaccountidentityverification
        description: accountidentityverificationgetaccountidentityverification
        call: docusign-identityverifications.accountidentityverificationgetaccountidentityverification
        with:
          accountId: rest.accountId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: docusign-identityverifications-mcp
    port: 9090
    transport: http
    description: MCP adapter for DocuSign REST API — IdentityVerifications. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: accountidentityverificationgetaccountidentityverification
      description: accountidentityverificationgetaccountidentityverification
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: docusign-identityverifications.accountidentityverificationgetaccountidentityverification
      with:
        accountId: tools.accountId
      outputParameters:
      - type: object
        mapping: $.
